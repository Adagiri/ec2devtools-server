extend type Query {
  server_types(
    instanceOption: AwsInstanceOption!
    region: String!
  ): [ServerType!]!
}

# extend type Mutation {
# }

type ServerType {
  type: String!
  monthlyCost: Float!
}

enum AwsInstanceOption {
  OnDemand
  Spot
}
